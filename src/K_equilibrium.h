int         is_equilibrium          (KConfig K);
int         diff_epsilon_KArray     (KConfig K,
                                     KArray& a1,
                                     KArray& a2,
                                     KScalar epsilon);
